name: Microservice Build

on:
  push:
    branches:
      - main
    paths:
      - 'applications/**'
      - '!**.md'
  workflow_dispatch:

jobs:
  detect-and-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Detect changed microservices
        id: detect
        run: |
          echo "Changed folders:"
          changed=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep '^applications/' | cut -d '/' -f2 | sort -u)
          echo "$changed"
          echo "services=$changed" >> "$GITHUB_OUTPUT"

      - name: Build changed microservices
        if: steps.detect.outputs.services != ''
        run: |
          IFS=$'\n'
          for service in ${{ steps.detect.outputs.services }}; do
            echo "➡️ Processing $service..."
            cd applications/$service
            echo "🧹 Running go mod tidy & vendor..."
            go mod tidy
            go mod vendor
            echo "🐳 Building Docker image using docker-compose-ext-server.yml..."
            cd ../../
            docker-compose -f docker-compose-ext-server.yml build $service
            echo "✅ Done with $service"
          done
